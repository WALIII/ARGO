function AR_BatchProcessing(function,level,scope)
% AR_BatchProcessing(function) put the (function) that you want to run across all directories
% Example: AR_BatchProcessing(AR_BatchDff_movfile,unprocessed,all_birds,today)
% this will run batch processing on all birds, every day.
